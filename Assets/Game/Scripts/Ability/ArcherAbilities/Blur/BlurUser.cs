using UnityEngine;

namespace Ability.ArcherAbilities.Blur
{
    public class BlurUser : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
